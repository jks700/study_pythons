당신은 20년차 프로그램코딩 전문가 입니다
다음과 같은 문제을 분석하여 사칙연산을 구현하는 함수로 만들어 주되 프로프트작성을 
JSON형식으로 만들어 주세요
[문제예시]
두 개의 숫자 리스트를 이용해 사칙연산(+, -, , /)을 수행하는 함수를 prompts/01_functions.*구현하시오.*
조건
테스트 데이터는 리스트(list) 로만 제공


테스트 데이터 개수는 10개


변수명은 소문자 + _ 조합


함수는 두 숫자를 입력받아 덧셈, 뺄셈, 곱셈, 나눗셈 결과를 모두 반환


0으로 나누는 경우는 "division_error" 반환



✅ 문제 코드 골격
학생이 채워 넣도록 비워둔 버전입니다.
def calculate_all(num1, num2):
    # 여기에 사칙연산 구현
    # return (덧셈, 뺄셈, 곱셈, 나눗셈)
    pass


# 테스트 리스트 (10개)
test_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]
test_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]

# 테스트 실행
for i in range(10):
    a = test_a[i]
    b = test_b[i]
    result = calculate_all(a, b)
    print(f"{a}, {b} => {result}")

{
  "prompt_title": "리스트 기반 두 숫자 사칙연산 함수 구현 (feat. 0으로 나누기 예외 처리)",
  "problem_description": "두 개의 숫자 리스트에서 원소를 하나씩 가져와 사칙연산(+, -, *, /)을 모두 수행하고 그 결과를 반환하는 함수 `calculate_all(num1, num2)`를 작성하시오.",
  "requirements": [
    "테스트 데이터는 두 개의 리스트(`test_a`, `test_b`)로만 제공됩니다.",
    "테스트 데이터의 개수는 10개입니다.",
    "변수명은 소문자와 언더스코어(_) 조합을 사용합니다.",
    "함수는 두 숫자를 입력받아 **덧셈, 뺄셈, 곱셈, 나눗셈 결과를 튜플 형태로 모두 반환**해야 합니다.",
    "**0으로 나누는 경우** 나눗셈 결과 대신 **문자열 'division_error'를 반환**해야 합니다."
  ],
  "student_task": "제시된 코드 골격의 `# 여기에 사칙연산 구현` 부분을 완성하세요.",
  "code_template": {
    "function_definition": "def calculate_all(num1, num2):",
    "implementation_hint": "# 여기에 사칙연산 구현\n# return (덧셈, 뺄셈, 곱셈, 나눗셈)",
    "test_data": {
      "test_a": "[10, 25, 40, 12, 7, 9, 16, 100, 3, 81]",
      "test_b": "[5, 5, 8, 3, 0, 3, 2, 4, 9, 9]"
    },
    "full_code_skeleton": [
      "def calculate_all(num1, num2):",
      "    # 여기에 사칙연산 구현",
      "    # return (덧셈, 뺄셈, 곱셈, 나눗셈)",
      "    pass",
      "",
      "# 테스트 리스트 (10개)",
      "test_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]",
      "test_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]",
      "",
      "# 테스트 실행",
      "for i in range(10):",
      "    a = test_a[i]",
      "    b = test_b[i]",
      "    result = calculate_all(a, b)",
      "    print(f'{a}, {b} => {result}')"
    ]
  },
  "expected_solution": {
    "function_implementation": [
      "    add_result = num1 + num2",
      "    sub_result = num1 - num2",
      "    mul_result = num1 * num2",
      "    if num2 == 0:",
      "        div_result = 'division_error'",
      "    else:",
      "        div_result = num1 / num2",
      "    return (add_result, sub_result, mul_result, div_result)"
    ]
  }
}
def calculate_all(num1, num2):
    # 덧셈, 뺄셈, 곱셈은 항상 가능
    add_result = num1 + num2
    sub_result = num1 - num2
    mul_result = num1 * num2
    
    # 나눗셈: 0으로 나누는 경우 예외 처리
    if num2 == 0:
        div_result = "division_error"
    else:
        # 일반적인 나눗셈 수행
        div_result = num1 / num2
        
    # 결과 튜플 반환
    return (add_result, sub_result, mul_result, div_result)


# 테스트 리스트 (10개)
test_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]
test_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]

# 테스트 실행
for i in range(10):
    a = test_a[i]
    b = test_b[i]
    result = calculate_all(a, b)
    print(f"{a}, {b} => {result}")